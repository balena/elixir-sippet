language: elixir
env: MIX_ENV=test
elixir:
  - 1.4
otp_release:
  - 19.0
script: mix coveralls.travis
after_script:
  - MIX_ENV=docs mix deps.get
  - MIX_ENV=docs mix inch.report
compiler:
  - gcc-4.8
